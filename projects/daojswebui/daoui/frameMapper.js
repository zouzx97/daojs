import {
  AppFrame,
  AppFrameEditor,
  DashboardFrame,
  DashboardFrameEditor,
} from '@daojs/ui';

export const frameEditorMapper = {
  AppFrame: AppFrameEditor,
  DashboardFrame: DashboardFrameEditor,
};

export const frameMapper = {
  AppFrame,
  DashboardFrame,
};
